# Installation

```bash
# Clone repo
git clone https://github.com/OCIoficial/oci-cms.git

# Install the package in the "python environment" where cms will run.
cd oci-cms
python setup.py install
```

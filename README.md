# Installation

```bash
# Clone repo
git clone https://github.com/OCIoficial/oci-cms.git

# Install the package in the same "python environment" than cms.
cd oci-cms
python setup.py install
```
